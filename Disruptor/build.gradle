description = 'A minimalist set of libraries to build trading systems using LMAX Disruptor'

dependencies {
   compile project(':core')

   compile group: 'com.lmax', name: 'disruptor', version: '3.2.1'
}
